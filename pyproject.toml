[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = [
    "E4", # Import yapısı hataları
    "E7", # Python statement hataları
    "E9", # Syntax/runtime hataları
    "F",  # Undefined name, kullanılmayan import vb.
    "I",  # Import sıralaması
]